<!DOCTYPE html>
<html>
<head>
  <title>Protected App | <%= content_for?(:title) ? yield(:title) : "Safe passwords" %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <%= render 'layouts/navbar' %>
  <div class="container">
    <div class="row-fluid">
      <div class="span12">
        <%= yield %>
      </div>
    </div>
  </div>
</body>
</html>
